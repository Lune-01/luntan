# RC1.9.4.4 全页面运行时审计

本轮从“个人信息打不开”开始，不再依赖纯静态测试。

实际找到并修复四类运行时错误：
1. 资料页引用辅助层跨作用域调用 `profileLevel()`。
2. 主题引用辅助层跨作用域调用 `allThreads()`。
3. 发帖引用辅助层跨作用域调用 `esc()`。
4. 更早版本遗留的未定义装饰函数：`ordinaryThreadStrip()` 与 `forumDetour()`，会让 relation.php / Helper / B库设置 / 归属修复等深层页面崩溃。

处理原则：
- 辅助UI不能阻断正文页面。
- 装饰性旧函数缺失时直接返回空串，不改变谜题逻辑。
- 引用记录单独隔离异常。

测试改为真实 Chromium：
- 顶部导航逐项点击。
- 六版块逐项点击。
- 普通主题 → 个人资料 → 发帖。
- 全部主要深层主线路由逐页渲染并要求 0 pageerror。
- 390×844 手机视口测试。

# RC1.9.4.5 引用/附上功能修复

根因：引用系统的 `addComposerRef()` / `removeComposerRef()` 位于主应用闭包外，却直接调用了闭包内部的 `save()`。
因此按钮本身已经绑定，但一点击就抛出 `ReferenceError: save is not defined`，导致：
- “引用本页发帖”点击后不能正常完成引用；
- “附上”点击后不能正常加入材料；
- 移除引用同样受影响。

修复：
- 引用辅助层改用独立 `persistReferenceState()`，直接通过既有 SideBStorage 保存同一存档键。
- 不再跨作用域调用 `save()`。
- 保留原有发帖、回复、引用渲染逻辑。

真实 Chromium 专项测试：
1. 打开个人资料；
2. 点击“引用本页发帖”；
3. 验证自动进入发帖区；
4. 验证资料页已经出现在“已附内容”；
5. 点击 × 移除；
6. 从下拉框选择最近页面并点“附上”；
7. 验证重新加入；
8. 真正发表主题；
9. 验证主题正文下出现“附：”；
10. 验证引用可点击。

以上全部通过且 0 pageerror。

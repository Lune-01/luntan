# BLIND TEST REPORT

## Test A｜普通玩家

实际使用 Headless Chromium，把 RC1.9 单页内容直接注入空白页后从登录开始操作；测试过程只通过页面可见文本、链接和输入框操作，不调用 QA 路由。

发现的问题：旧版全局导航会把调查页面逐个暴露出来；2019 迁移帖原本有直达同名旧帖的“同期开贴”跳板；搜索系统会根据刚浏览的内容自动整理关键词；普通背景帖过于整齐；打印页存在无返回入口。

修改方式：删除调查导航、自动搜索词、进度型 recent-topic 和 visit-count PM；同名旧帖改为在存档版正常出现；最近查看/搜索历史只机械记录真实操作；补上打印页返回主题；普通帖子加入无意义和失败内容。

修改后结果：登录后的顶栏只剩论坛首页、我的主页、短消息、搜索、关于；玩家可以从旧帖存档自行看到 2019 迁移说明，并在存档版再次发现同名旧帖；异常没有视觉标签；搜索“打印机”后搜索记录只出现“打印机”，不会自动冒出 Adam/1848 等词。浏览器实测 PASS。

放弃风险：早期比 RC1.8.3 更需要玩家主动逛旧帖存档。主线仍有两类自然入口——存档版浏览和玩家主动搜索 UID/用户名——但不再用 NPC page-count 兜底强行拉回。

## Test B｜高水平网络解谜玩家

主动测试 hash 猜页、错误关键词和提前访问后期页面。

结果：`#wjc`、`#code17`、`#incident2018`、`#helper`、`#snapshots`、`#candidate`、`#edges` 均可提前进入；没有“尚未解锁 / 证据不足 / 需要完成前置任务”提示。提前进入时页面只展示当时文件本身能展示的内容，例如 candidate 在缺 CSV 时只显示脚本和“目录里没有 CSV”，不会替玩家补上下文。错误搜索“硬盘型号X9”正常返回无结果，不损坏状态。PASS。

## Test C｜乱点玩家

Headless Chromium 随机点击 90 次，混合论坛首页、普通帖、附件、打印页、RSS、PM、搜索、关于和返回操作，并周期性重绘页面。

结果：90 次随机操作无白屏；账号仍保持注册状态；路由保持有效。此前发现的普通打印页无返回入口已修复。随后重新运行旧存档/主线模拟：谨慎玩家、不保存旧证据、错误 snapshot、反复刷新、乱搜司南、乱改签名、错误 Identity Edge、跳过 1094 修复均未产生死档。PASS。

## 额外回归

- `qa.js`：PASS
- `qa_reachability.js`：PASS
- `player_simulation.js`：10/10 PASS
- `player_simulation_adversarial.js`：10/10 PASS
- `blind_reasoning_test.js`：PASS
- Node syntax：PASS


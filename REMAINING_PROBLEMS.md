# REMAINING PROBLEMS

1. 少数剧情 PM 仍然由“玩家完成了一个真实相关操作”触发，例如查看 future DB 后 Hanlexios 发来 Adam 删除记录、解除 2023 关联后火山/E 的后续消息。这一轮已经删除所有“访问 N 页后正好来消息”的机制，但这些操作关联型回应仍然存在；它们属于论坛人物对真实事件的回应，不再承担唯一主线入口。
2. 普通帖子、附件、打印页、RSS 仍有一部分是直接替换 `#app` 的旧实现，而不是完整 History API 页面。站内“返回主题/论坛首页”已补齐，随机点击不会死路，但手机浏览器硬件 Back 在所有这些旧页面上仍不能保证完全等价于站内返回。
3. 自动化盲测能验证入口、跳关、死档和明显导演式 UI，但不能替代真正第一次接触 Side-B 的真人对“我为什么会想到点这里”的主观判断。代码层目前未发现新的硬锁。
